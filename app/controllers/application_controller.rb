# frozen_string_literal: true

# Base controller for application. Any logic put in here applies to all controllers which are a subclass of
# `ApplicationController`, which includes a majority, if not all, of the controllers.
class ApplicationController < ActionController::Base
end
